{
  "name": "winadmin",
  "version": "1.0.0",
  "description": "1, npm i http-server --save-dev 2, npm run dev",
  "scripts": {
    "dev": "http-server ./src -p 8090 -o"
  },
  "author": "rocching",
  "devDependencies": {
    "http-server": "^0.9.0"
  }
}
